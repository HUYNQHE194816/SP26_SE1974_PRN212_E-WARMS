<Window x:Class="WarrantyRepairCenter.EmployeeWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Employee Management" Height="500" Width="620">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Border HorizontalAlignment="Stretch" Background="LightSteelBlue">
                <Label Content="Employee Management" FontSize="25"
                       HorizontalAlignment="Center" Margin="5" FontWeight="Bold"/>
            </Border>

            <Grid HorizontalAlignment="Stretch" Margin="10,5,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Margin="5" Content="Full Name:"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5" x:Name="txtFullName"/>

                <Label Grid.Row="1" Grid.Column="0" Margin="5" Content="Username:"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5" x:Name="txtUsername"/>

                <Label Grid.Row="2" Grid.Column="0" Margin="5" Content="Password:"/>
                <PasswordBox Grid.Row="2" Grid.Column="1" Margin="5" x:Name="pwdPassword"/>

                <Label Grid.Row="3" Grid.Column="0" Margin="5" Content="Role:"/>
                <ComboBox Grid.Row="3" Grid.Column="1" Margin="5" x:Name="cboRole"/>
            </Grid>

            <StackPanel Margin="5" HorizontalAlignment="Center" Orientation="Horizontal">
                <Button x:Name="btnRefresh" Content="Refresh"
                        Height="30" MinWidth="80" Click="btnRefresh_Click"/>
                <Button x:Name="btnAdd" Content="Add"
                        Margin="10,0,0,0" Height="30" MinWidth="80" Click="btnAdd_Click"/>
                <Button x:Name="btnEdit" Content="Edit"
                        Margin="10,0,0,0" Height="30" MinWidth="80" Click="btnEdit_Click"/>
                <Button x:Name="btnChangePass" Content="Change Password"
                        Margin="10,0,0,0" Height="30" MinWidth="110" Click="btnChangePass_Click"/>
                <Button x:Name="btnRemove" Content="Remove"
                        Margin="10,0,0,0" Height="30" MinWidth="80" Click="btnRemove_Click"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dgData" Margin="5"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionChanged="dgData_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="30*"/>
                <DataGridTextColumn Header="Username"  Binding="{Binding Username}"  Width="20*"/>
                <DataGridTextColumn Header="Role"      Binding="{Binding Role}"      Width="15*"/>
                <DataGridTextColumn Header="Created"
                                    Binding="{Binding CreationTime, StringFormat=\{0:yyyy-MM-dd\}}"
                                    Width="15*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>